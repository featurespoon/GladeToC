#include <gtk/gtk.h>
#include "top_window.h"
#include "top_window_gui.h"
#include "file_choose2_gui.h"
#include "file_choose1_gui.h"
#include "file_choose3_gui.h"

void initialise_top_window (TopWindowGui *top_window)
{

	return;
}


void
glade_click (GtkButton* widget,
	gpointer                  user_data)

{
	/* TopWindow *prog = (TopWindow*) g_object_get_data (G_OBJECT (widget), "owner"); */

	return;
}

void
folder_click (GtkButton* widget,
	gpointer                  user_data)

{
	/* TopWindow *prog = (TopWindow*) g_object_get_data (G_OBJECT (widget), "owner"); */

	return;
}

void
resource_click (GtkButton* widget,
	gpointer                  user_data)

{
	/* TopWindow *prog = (TopWindow*) g_object_get_data (G_OBJECT (widget), "owner"); */

	return;
}

void
generate_click (GtkButton* widget,
	gpointer                  user_data)

{
	/* TopWindow *prog = (TopWindow*) g_object_get_data (G_OBJECT (widget), "owner"); */

	return;
}

void
regenerate_clicked (GtkButton* widget,
	gpointer                  user_data)

{
	/* TopWindow *prog = (TopWindow*) g_object_get_data (G_OBJECT (widget), "owner"); */

	return;
}

void
dismiss_click (GtkButton* widget,
	gpointer                  user_data)

{
	/* TopWindow *prog = (TopWindow*) g_object_get_data (G_OBJECT (widget), "owner"); */

	return;
}

	/*  Signal handlers for Dialogue file_choose2 */
void
choose2_click (GtkButton* widget,
	gpointer                  user_data)

{
	/* TopWindow *prog = (TopWindow*) g_object_get_data (G_OBJECT (widget), "owner"); */

	return;
}

	/*  Signal handlers for Dialogue file_choose1 */
void
choose1_click (GtkButton* widget,
	gpointer                  user_data)

{
	/* TopWindow *prog = (TopWindow*) g_object_get_data (G_OBJECT (widget), "owner"); */

	return;
}

	/*  Signal handlers for Dialogue file_choose3 */
void
choose3_clicked (GtkButton* widget,
	gpointer                  user_data)

{
	/* TopWindow *prog = (TopWindow*) g_object_get_data (G_OBJECT (widget), "owner"); */

	return;
}


	/*  Model populaters */

